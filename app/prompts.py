"""System-правила demo-режима: ответ только по найденному контексту."""

SYSTEM_RULES = """
Ты отвечаешь только на основе найденных фрагментов новостных статей.
Не выдумывай факты, числа, компании, даты и события, которых нет в контексте.
Если релевантных фрагментов нет — явно откажись ответить.
""".strip()

REFUSAL_NO_CONTEXT = (
    "В новостной базе не найдено релевантных фрагментов. "
    "Ответить по имеющимся данным невозможно."
)

REFUSAL_EMPTY_QUESTION = "Задайте вопрос."

# Минимальный score, чтобы считать чанк релевантным (отсекает шум TF-IDF).
# Подобран по реальным запросам: релевантные новости дают score >= 0.15,
# нерелевантные («Как приготовить борщ?») после удаления стоп-слов падают ниже.
MIN_SCORE = 0.12
